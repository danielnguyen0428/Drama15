/**
 * `@drama15/test-helpers` — shared fast-check generators and vitest project
 * configuration for property tests across the Drama15 commercial web SaaS
 * workspace (`apps/api`, `apps/web`, `apps/admin`).
 *
 * Validates: testing strategy (commercial-web-saas tasks 1.5).
 */

export * from './_helpers.js';
export * from './vitestProjectsConfig.js';
